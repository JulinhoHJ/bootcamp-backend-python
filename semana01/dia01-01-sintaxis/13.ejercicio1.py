# Escribe un programa que halle el área de un triangulo
# solicitando la base y la altura
# A(T) = (base * altura) / 2

base = float(input("Ingrese la base: "))
altura = float(input("Ingrese la altura: "))

area = (base * altura) / 2
print(f"El área del triangulo es: {area}")