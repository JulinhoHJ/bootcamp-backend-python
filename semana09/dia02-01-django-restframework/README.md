# pip install python-dotenv -> instalar el paquete de configuración de entorno
# pip install psycopg2-binary -> instalar el paquete de psycopg2

# pip install djangorestframework -> instalar el paquete de django rest framework

## RUTA

# Primero creamos los modelos
# Segundo creamos los serializers
# Tercero creamos los views (importamos los serializers y los modelos)
# Cuarto creamos la urls (endpoint)
# Quinto importamos en el enrutador principal (django_restframework/urls.py)

Nota: en settings.py ponemos la ruta de los modelos y los serializers